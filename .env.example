# Copy to .env.local and fill in your keys. DO NOT COMMIT .env.local
# Google Maps: used by app and by backend for Places search, place details (hours), and Distance Matrix (LLM realistic itinerary tools)
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your_key_here
# OpenRouter (google/gemini-3-flash-preview): used when someone types @gemini in trip chat (backend loads from .env.local)
EXPO_PUBLIC_OPENROUTER_API_KEY=your_key_here
OPENROUTER_API_KEY=
EXPO_PUBLIC_CONVEX_URL=your_convex_url_here

# Custom chat backend. REQUIRED for Join/chat on iPhone or other devices: use your PC's LAN IP (e.g. http://192.168.1.5:8000). localhost only works in browser.
EXPO_PUBLIC_CHAT_WS_BASE=http://192.168.1.17:8000

# Invite link base for "Join trip" (optional). Use your machine's LAN IP so other devices can open the link in Expo Go, e.g. exp://192.168.1.5:8081/--
EXPO_PUBLIC_INVITE_BASE=
